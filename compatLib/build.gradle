plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace "com.jeremy.lawnchair.compatlib"

    buildFeatures {
        aidl true
    }

    sourceSets {
        main {
            aidl.srcDirs = ['src/main/java']
        }
    }
}

addFrameworkJar('framework-15.jar')
